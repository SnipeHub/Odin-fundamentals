package math_utils
// also make sure this is in a sub directory called math_utils

// Function to add two integers
add :: proc(a, b: int) -> int
{
    return a + b
}

// Function to subtract two integers
subtract :: proc(a, b: int) -> int
{
    return a - b
}